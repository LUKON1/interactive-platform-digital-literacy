# Интерактивная платформа для освоения цифровой грамотности

[🎥 Посмотреть видео-демонстрацию функционала](https://disk.yandex.ru/i/FNLRu5z3MBVGDw)

Проект разработан в рамках Московской предпрофессиональной олимпиады школьников (2025-2026 учебный год). Это веб-приложение, предназначенное для обучения основам цифровой безопасности, защиты данных и критического мышления в сети через интерактивные игровые механики.

## 🚀 Функционал и Особенности

Приложение состоит из набора интерактивных уроков, объединенных в дерево навыков. Основные темы:

- **Разбор URL и фишинг**: Учимся анализировать структуру ссылок.
- **Цифровой след**: Понимаем, как наши действия формируют онлайн-портрет.
- **Парольная защита**: Создаем надежные пароли и понимаем принципы энтропии.
- **Deepfakes**: Учимся отличать настоящий контент от сгенерированного ИИ. (Используются видео-материалы).
- **Wi-Fi безопасность**: Симулятор подключения к публичным сетям.

### Технический стек

- **Frontend**: React 19, Vite
- **Стилизация**: Tailwind CSS v4, Motion (Framer Motion)
- **Состояние**: Zustand
- **Навигация**: React Router v7
- **Интерактивность**: dnd-kit (drag & drop), кастомные игровые механики.

### Оптимизация

В проекте реализованы следующие оптимизации для повышения производительности и удобства:

- **Code Splitting (Lazy Loading)**: Разделение кода страниц для ускорения начальной загрузки. Основные маршруты загружаются динамически.
- **Assets Optimization**: Использование современных форматов и оптимизация изображений.
- **Design System**: Единая система дизайн-токенов через Tailwind CSS v4.

## 🛠 Установка и Запуск

Для запуска проекта локально вам потребуется Node.js (рекомендуется v18+).

1.  **Клонировать репозиторий** (если применимо) или распаковать архив:

    ```bash
    git clone <ссылка-на-репозиторий>
    cd interactive-platform-digital-literacy
    ```

2.  **Установить зависимости**:

    ```bash
    npm install
    ```

3.  **Запустить режим разработки**:

    ```bash
    npm run dev
    ```

    Приложение будет доступно по адресу `http://localhost:5173`

4.  **Сборка для продакшена**:

    ```bash
    npm run build
    ```

    Собранные файлы будут находиться в папке `dist`

5.  **Локальный просмотр сборки**:

    ```bash
    npm run preview
    ```

6.  **Быстрый запуск через Docker (Рекомендуется)**:

    **ТОЛЬКО если у вас установлен Docker**, вы можете запустить production-версию приложения одной командой без установки Node.js:

    ```bash
    docker compose up --build
    ```

    Приложение будет доступно по адресу `http://localhost:8080`

© 2026 Проектная работа
